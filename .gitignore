# 의존성 (필요 시 재설치: npm install / pip install)
/node_modules/
/.venv/
__pycache__/
*.pyc

# 큰 설치 파일
*.exe

# 개인 데이터 — 각자 PC에만 보관 (앱의 백업 기능으로 따로 백업)
/data.json
/data.json.tmp

# 로컬 전용 설정 (민감 정보 가능)
/.claude/settings.local.json

# OS / 에디터 부산물
Thumbs.db
.DS_Store
